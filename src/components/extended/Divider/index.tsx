import { Spacer } from '@/components/base';

export default function Divider({}) {
  return (
    <Spacer
      size={'full'}
      axis={'horizontal'}
      background={'wordHighlight'}
      height={'px'}
      marginTop={'0'}
      marginBottom={'8'}
      marginLeft={'auto'}
      marginRight={'auto'}
    />
  );
}
